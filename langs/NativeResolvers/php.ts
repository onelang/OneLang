class PhpArray<T> {
    _one: OneArray<T>;
}

class PhpMap<K,V> {
    _one: OneMap<K,V>;
}

class PhpString {
    _one: OneString;
}

class PhpBoolean {
    _one: OneBoolean;
}

class PhpNumber {
    _one: OneNumber;
}