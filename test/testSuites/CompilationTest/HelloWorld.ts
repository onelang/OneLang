class TestClass {
    testMethod() {
        const value = 1 + 2 * 3 - 4;
        const map_ = { a:5, b:6 };
        const text = `Hello world! value = ${value}, map[a] = ${map_["a"]}`;
        console.log(text);
    }
}
