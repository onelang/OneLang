class TestClass {
    testMethod() {
        const result: string[] = ["y"];
        const map = { x:5 };
        const keys = Object.keys(map);
        console.log(result[0]);
        console.log(keys[0]);
    }
}
