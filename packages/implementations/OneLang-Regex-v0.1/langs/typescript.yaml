implements:
  - interface: { name: One.Regex, minver: 0.1, maxver: 0.1 }
    language: typescript
    implementation:
      classes:
        OneRegex:
          includes: ["one"]
          template: "one.Regex"
