implements:
  - interface: { name: One.Regex, minver: 0.1, maxver: 0.1 }
    language: java
    native-include-dir: java
    implementation:
      classes:
        OneRegex:
          includes: ["io.onelang.OneRegex"]